"""
MiroFish v2 — schema-direct, no-fork rebuild.

Path A: deterministic CSV/JSON ingestion with no LLM. The new code lives here
so the legacy LLM-driven services in ``app.services`` can be retired step by
step without breaking the running backend.
"""
